# Encode IG — S2 Windows Python Shim
## Instagram caption

---

**Primary caption:**

4/10.

Day 2 of CS50P. Plan was Python installed, course enrolled, Lecture 0 watched. Reality was 45 minutes fighting the Windows App Execution Alias — the shim that makes `python` open the Microsoft Store instead of launching an interpreter.

Zero lecture. Zero problem set. Zero reading.

The streak stayed alive and I documented the fix as an evergreen reference so NightOwl and Ironridge builds won't hit it again. That's the only reason this isn't a 2.

Tooling is not learning. I'm not going to pretend it was.

Two rules out of today:
Tooling time is capped at 1 session per week.
The streak number is splitting into two tracks — sessions logged vs. sessions that moved curriculum forward.

A streak that counts tooling at full weight lies about the pace.

Full post at tortoise.io / link in bio.

---

**Hashtags (5-8):**
#buildinpublic #learninginpublic #cs50 #python #windows #autodidact #tortoisebuild

---

**Alt text for image (if using):**
Screenshot of PowerShell showing `python --version` opening the Microsoft Store page instead of returning a version number, with Settings menu highlighted showing App Execution Aliases toggled off for python.exe.
